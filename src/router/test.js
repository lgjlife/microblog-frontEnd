import Echarts from '@/components/chart/Echarts.vue'

let routers =[

    {
        path: '/chart',
        component: Echarts,
        meta: {
            title: "图标"
        }
    },
]

export  default routers;