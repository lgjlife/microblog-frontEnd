let　menuItem = {
    monitor:{
        // 注册中心
        REGISTER_CENTER_MONITOR: "REGISTER_CENTER_MONITOR",
        DRUID_MONITOR: "DRUID_MONITOR",
        SPRING_BOOT_ADMIN_MONITOR: "SPRING_BOOT_ADMIN_MONITOR",
        ROCKETMQ_MONITOR_MONITOR: "ROCKETMQ_MONITOR_MONITOR",
        API_LIST: "API_LIST"
    }
}


export default {
    menuItem,
}